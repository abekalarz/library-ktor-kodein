
services:
  db:
    image: mariadb:11
    container_name: library-db
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
    ports:
      - "${MYSQL_PORT}:3306"
#  app:
#    build: .
#    container_name: library-app
#    environment:
#      DB_URL: jdbc:mariadb://db:3306/librarydb
#      DB_USER: root
#      DB_PASS: root
#    ports:
#      - "8080:8080"
#    depends_on:
#      - db
